spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/xht_activiti8?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: yzd2021
  activiti:
    db-history-used: true
    history-level: full
    database-schema-update: true
    check-process-definitions: false
    deployment-mode: never-fail

  security:
    user:
      name: user
      password: 123456

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

logging:
  level:
    org.activiti.engine.impl.persistence.entity: DEBUG